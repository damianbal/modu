import Component from "../Component"

export default class CollisionComponent extends Component {

    constructor() {
        super();

        this.name = "collision"
        
    }

    update(dt) {
        super.update(dt)
    }

}